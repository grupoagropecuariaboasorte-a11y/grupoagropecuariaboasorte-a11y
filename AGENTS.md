# Instruções e Regras do Projeto

- **Git & GitHub Automático**: Ao final de cada ciclo de alterações ou implementações concluídas no código, realize o `git add .`, crie um `git commit` descritivo e faça o `git push origin main` para manter o repositório do GitHub sempre sincronizado.
